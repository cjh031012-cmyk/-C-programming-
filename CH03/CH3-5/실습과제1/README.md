1. 코드 중복을 줄여서 코드 길이를 줄이기 위해서 사용합니다.
2. 함수가 호출 될때 함수가 실행되기 전 후로 메모리 할당, 매개변수 초기화 같은 일들이 생기는 일 (짧은 코드들은 오버헤드때문에 함수호출이 비효율적)
3. 오류가 아닙니다. 클래스의 멤버 함수로서 권한을 유지하기 때문에 private 변수에도 접근 가능합니다.
4. 위에서 2번에서 말한 오버헤드가 발생하는 함수들 때문에 사용하는건데 함수의 장점과 속도를 둘 다 쓸 수 있게 하기 위해 만들었습니다.
