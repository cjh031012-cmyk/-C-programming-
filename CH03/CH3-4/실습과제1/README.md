1. 데이터 요염을 막고, 사용자가 이상한 값을 넣는 걸 막을 수 있고, 코드 유지보수가 쉽습니다.
2. 생성자는 객체가 생성되는 순간 바로 멤버변수를 초기화 하고, setter는 함수가 호출될때 지정된 멤버변수를 원하는 값으로 초기화 할 수 있습니다.
3. 캡슐화: 데이터(변수) + 기능(함수)를 하나로 묶는 것이고 관리하기가 쉽습니다.
   정보은닉: 중요한 데이터나 내부 구현을 외부에서 못보게 숨기는 것입니다.
